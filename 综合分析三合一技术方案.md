# 综合分析三合一：操作流程与技术方案

## 目标

将“市场仪表盘”“综合总览”“风险中心”合并为一个“综合分析”工作区，保留现有成本计算器、运输辅助、策略生成和晨报。页面的任务不是堆放图表，而是把平台已有数据按“客观事实 → 业务关注 → 市场对比 → 总结与风险”逐层压缩。

## 操作流程

1. 进入“综合分析”，页面读取当前产品线、区域下的行情、政策、内部聚合、成本、汇率情景和风险信号。
2. 在页面顶部使用搜索框和信息类型筛选，快速定位原始记录、政策、经营聚合或风险指标。
3. 先查看“客观信息”：保留来源、时间、抓取方式、当前值、基线和状态等事实字段。
4. 再查看“处理后指标”：展示变化幅度、完成率、成本敏感度、政策数量和风险数量等由现有数据计算出的指标。
5. 查看“业务关注点”：将事实压缩成订单、成本、市场维护、目标完成和合规等关注项；当前数据没有客户重要度、利润等字段时，明确标记为“暂无接入”。
6. 查看“市场对比与内外部综合”：按数据来源、区域、产品线和内部聚合进行对照，不虚构集团内部或国内钢厂数据。
7. 最后查看“总结与风险信号”：风险信号只取自现有 `risk_signals.json` 及前面客观数据的变化，保留原有风险级别与人工审核状态。

## 技术方案

- 新增 `src/features/analysis/UnifiedAnalysis.tsx`，集中调用现有 `DataProvider`，不改变任何数据接口和字段关系。
- 新增 `src/features/analysis/UnifiedAnalysis.css`，采用紧凑的分层信息流；不新增持续动画、不依赖装饰光晕，正文保持 16px 左右。
- 顶部搜索框在客户端对已加载客观记录进行文本匹配，匹配来源、指标、标题、区域、产品线和风险级别。
- 处理后指标只使用当前已有字段计算：行情相对基线变化、内部销量完成率、单位成本均值、汇率情景区间、政策待核验数、风险级别数量。
- 旧路径 `/dashboard`、`/overview`、`/risk-center` 指向新的综合分析页，保证已有书签和入口不失效；导航只保留一个“综合分析”。
- 成本计算器、运输辅助、策略生成和晨报组件不改动其业务计算、筛选、评分及审核逻辑。

## 验收标准

- 信息顺序固定为：客观信息 → 处理后指标 → 业务关注点 → 市场对比 → 总结与风险。
- 搜索框能同时过滤客观信息与后续关注项，并显示匹配数量。
- 页面不再重复出现三套市场、总览、风险入口。
- 1280px 下内容密度适中，重要指标和风险结论优先；不出现无意义大块空白。
- 关键正文、表格和控件不低于 14px，主体说明约 16px。
- 风险信号仍支持查看详情和确认/忽略；数据接口、成本公式、运输路线评分、策略规则和晨报流程不变。
- `npm run type-check`、`npm run build` 通过，7 个旧路径及 4 个保留功能页面可以加载。

## 02—05 图表专项验收补充

- 02 处理后指标：经营目标完成率必须在单一统计周期下按结束日期形成趋势；汇率敏感度按货币对独立归一化，不混合绝对汇率。图表标题或 Tooltip 应说明周期、经营量与目标量。
- 03 业务关注点：数据准备图只表达字段是否接入；客户重要度、订单利润、维护程度等缺失字段显示“未接入”，不得以 0% 伪装业务评分。客户分群与订单结构图只使用同一经营周期的 `volume_t`。
- 04 市场对比与内外部综合：市场区间图只有在 `indicator_code`、`unit`、`currency`、`frequency`、日期完全一致且来源数大于 1 时绘制；政策图只称“政策覆盖区域”，不替代贸易伙伴地图。
- 05 总结与风险信号：风险趋势、风险散点和风险评分排名必须使用相同的 `review_status !== dismissed` 口径；散点与排名 Tooltip 保留 `signal_id`、`rule_id`、`evidence_ref` 追溯信息。
- 图表可读性：中文标题不低于 16px，图例/坐标轴/Tooltip 不低于 13px；主图高度 260—360px；主题切换同步背景、网格、文字与风险颜色；无数据显示明确空状态；移动端不产生页面级横向滚动。
