# 模型部署建议

## 当前状态

当前项目没有训练好的模型文件，也没有实际的模型加载/推理代码。`ModelManifest` 和 `modelStatus` 只是类型与状态预留，`onnxruntime-web` 仅作为依赖存在；因此不能把当前版本宣称为“已部署机器学习模型”。

## 推荐部署顺序

1. **先用确定性规则上线**：数据校验、风险阈值、成本计算、路线评分必须由 TypeScript/Python 规则完成，并保留输入、版本和输出。
2. **离线训练**：用历史行情、运费、政策和人工审核结果训练异常检测/风险分类模型，按时间切分训练集和验证集，避免未来数据泄漏。
3. **服务端推理优先**：企业内网建议用 Python FastAPI + scikit-learn/LightGBM 服务模型，前端只调用 `/api/predict`，模型文件放在受控服务器或对象存储中。
4. **需要离线演示时再用 ONNX Web**：将经过验证的轻量模型导出 ONNX，放入 `public/models/`，加载前校验 manifest、特征顺序、模型版本和数据快照哈希。

## 适合本项目的模型

- 异常检测：Isolation Forest 或基于滚动窗口的 Z-Score/CUSUM。
- 风险分类：Logistic Regression、Random Forest、LightGBM；优先可解释模型。
- 价格/运费预测：先用 ETS、ARIMA，再与 LightGBM 滞后特征模型比较。
- LLM：只用于网页字段抽取、政策摘要和证据归纳，不直接修改数值或替代最终计算。

## 最低生产要求

- `model_id`、版本、训练时间、特征列表、训练样本区间、指标和阈值写入 manifest。
- 推理结果保存模型版本、输入快照 ID、置信度和人工审核状态。
- 监控缺失率、漂移、误报/漏报、延迟和失败率。
- 模型不可用时自动回退到规则引擎，并在界面明确标注。
- 生产环境不要把企业内部数据直接发往外部模型服务；如需 LLM，优先部署内网模型或脱敏后调用。
